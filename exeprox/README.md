# Exeprox

Exeprox is a reverse proxy that handling incoming HTTP requests.
Requests are proxied to exed or to an exelet machine based on the Host
and peer IP.

In the future exeproc may also handling incoming SSH requests.

The goal is that different exeprox instances will run around the world
to provide faster access to VMs hosted on local exelets.
