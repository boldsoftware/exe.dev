# undercover

Multi-input Go coverage visualization tool.

This is vibe coded. The intention is to build coverage files per test, and then
you can see which test coverage came from.

# Usage

```bash
undercover -o output.html input1.cov input2.cov [input3.cov ...]
```
