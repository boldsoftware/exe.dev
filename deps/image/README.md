# Image
This is vendored from github.com/estesp/manifest-tool to work around some deprecated imports (distribution/reference).
It is used to extract content from an OCI image into a destination directory. It contains a content store that uses a 
temporary directory to store content instead of in-memory.
