# generate-gateway-token

This directory contains the source for a command that will generate a
bearer token for use with package llmgateway. This source should not be
included in the image. The natively-compiled static binary for this command
should be built and included in the image, instead.
