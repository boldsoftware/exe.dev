# Welcome Server

This is a placeholder web server for exe.dev boxes.

If you start an exe.dev welcome image, this web server will be in /app, and
will have been started by systemd. Disable it with "systemd stop webserver" and
"systemd disable webserver".
