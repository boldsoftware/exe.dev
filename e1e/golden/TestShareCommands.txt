

●
● ssh localhost
●

~~~ EXE.DEV ~~~

EXE.DEV: get a box over ssh
To sign up, please verify your email.

Please enter your email address: TestShareCommands@example.com

[DEV-ONLY] Emailed link: http://localhost:EXED_HTTP_PORT/verify-email?token=EMAIL_VERIFICATION_TOKEN&s=exemenu


Verification email sent to: TestShareCommands@example.com
Pairing code: EMAIL_VERIFICATION_CODE
Waiting for email verification...
✓ Email verified successfully!


Registration complete!

Your account has been successfully created.




Welcome to EXE.DEV!

- new to create your first box
- help to see a list of commands

exe.dev ▶ whoami
Email Address: TestShareCommands@example.com
SSH Keys:
  Public Key: SSH_PUBKEY ← current
exe.dev ▶ new --name=BOX_NAME --command="/bin/bash"
Creating BOX_NAME (medium) using image boldsoftware/exeuntu...
Ready in ELAPSED_TIME!

App (HTTPS proxy → :8000)
http://BOX_NAME.localhost:EXED_HTTP_PORT

SSH
ssh -p SSH_PORT BOX_NAME@localhost

exe.dev ▶ ls
Your boxes:
  • BOX_NAME - running (boldsoftware/exeuntu@sha256:IMAGE_HASH)
exe.dev ▶ share show BOX_NAME

Sharing for box 'BOX_NAME'
URL: http://BOX_NAME.localhost:EXED_HTTP_PORT
Mode: Private

No shares configured.

To share with someone, use:
  share add BOX_NAME user@example.com
  share add-link BOX_NAME

exe.dev ▶ share add BOX_NAME friend@example.com --message='Welcome to my box'

✓ Invitation sent to friend@example.com

friend@example.com will receive an email with access instructions.
You can also share this URL directly: http://BOX_NAME.localhost:EXED_HTTP_PORT
(They'll need to log in with friend@example.com to access it)

exe.dev ▶ share show BOX_NAME

Sharing for box 'BOX_NAME'
URL: http://BOX_NAME.localhost:EXED_HTTP_PORT
Mode: Private

Shared with users:
  friend@example.com (invited INVITE_AGE)

No share links yet. Create one with:
  share add-link BOX_NAME

exe.dev ▶ share add-share-link BOX_NAME

✓ Share link created

Anyone with this link can access your box after logging in:
http://BOX_NAME.localhost:EXED_HTTP_PORT?share=SHARE_TOKEN

To revoke this link, use:
  share remove-link BOX_NAME SHARE_TOKEN

exe.dev ▶ share show BOX_NAME

Sharing for box 'BOX_NAME'
URL: http://BOX_NAME.localhost:EXED_HTTP_PORT
Mode: Private

Shared with users:
  friend@example.com (invited INVITE_AGE)

Share links:
  SHARE_TOKEN (created SHARE_AGE, used 0 times)

exe.dev ▶ share remove BOX_NAME friend@example.com

✓ Removed friend@example.com's access to 'BOX_NAME'

exe.dev ▶ share show BOX_NAME

Sharing for box 'BOX_NAME'
URL: http://BOX_NAME.localhost:EXED_HTTP_PORT
Mode: Private

No users shared yet.

Share links:
  SHARE_TOKEN (created SHARE_AGE, used 0 times)

exe.dev ▶ help share

Command: share

Share box access with others

Usage: share <subcommand> <box> [args...]

Options:
  --json output in JSON format

Subcommands:
  show          - Show current shares for a box
  add           - Share box with a user via email
  remove        - Revoke a user's access to a box
  add-link      - Create a shareable link for a box
  remove-link   - Revoke a shareable link
exe.dev ▶ rm BOX_NAME
Deleting BOX_NAME...
Box "BOX_NAME" deleted successfully
exe.dev ▶ exit
Goodbye!
