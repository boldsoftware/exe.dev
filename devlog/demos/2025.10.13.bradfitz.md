Demo by: Blake and Crawshaw

---

Got sign-in page

Auth token in terminal URL

Did not hit confirm again.. Got confused

Kill “confirm” with fire.

Don’t mention port 8000 in menu link on new

He found welcomed (dubious it is running)

Menu system could be less “shell” like to distinguish between it and a ssh session

Plays with billing commands in menu (we should remove these for now)

Remove aliases

Feels to Brad like Cisco router

Did not expect “new” to do something without args (panics and ctrl-c and kills menu session)

Remove “rm” in menu. Feels like /bin/rm. Brad tries “rm *“ — “Delete” is good enough and distinguished.

Wants to “ssh” from menu

“I can use -size on new, but can’t see it in list -json???” (remove -size)

“Hooking this up for automation over ssh isn’t great outside Go” - Brad on wanting rest api

IPv6?? Give me my own - Brad – (David is worried about people relying on it)

SNI proxy - I want tcp/udp (we can do this per sub domain and port)

To me (Blake) is like to do this by getting a list of all things bound to a port and “make public” in UI or have easy command from VM (assuming exeuntu) — something like ‘expose 8022’

I want a “network sidecar”

I want to give out untrusted VMs to customers but I want to contain their network

I want a working terminal without a username/password

He also tried Shelly and scratched his head a bit with it. David saw more of this than I did. I was catching up on notes.

Brad asks if we support websockets, and asked if we used httputil reverse proxy and then told us we did because we use that. — We should be sure to document we support websockets
