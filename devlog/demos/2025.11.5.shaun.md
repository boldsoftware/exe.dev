Demo by: Josh
Git version: c261f89a5233f3ac9340a06327518eaaeca25830

---

Shaun is a recent CS graduate who also has some past sysadmin experience.

Went to website, immediately switched to terminal.

sshed in from a vm, doesn't trust random stuff.

Oops, no key in vm. No prob, ssh-keygen.

Had to delete weird rgb terminal spam before entering email address. [Fixed now.](https://github.com/boldsoftware/exe/commit/46fd53f5a7db5ce27686c044c3c37766b62bb38a)

[Email didn't get delivered to icloud email address](https://github.com/boldsoftware/exe/issues/75). gmail worked.📍

Auto-unblocked ssh: "oooh"

Ran `help`, skimmed.

"Let's start a new box."

It [failed](https://github.com/boldsoftware/exe/issues/71). I asked him to retry.

Worked second time, was fast. (I had run `new` recently so there was no image pulling delay.)

Unsure how to pronounce exeuntu. (Evan asked about that too at some point. Might be nice to have a more pronounceable name?)

Went to welcome page...but email is set up only on his phone.

Tried to continue on his phone, got stuck somewhere. (I couldn't see exactly what happened.) Energy stalled out a bit there. Not sure what to do here, but not ideal.📍

He went back to exploring the VM side.

"Hmm, I assume it's a full VM? Let's find out."

Went back to terminal. Wasn't in the VM any more, which meant he had a new key.

That meant that `ssh box@exe.dev` took him through a registration flow again. He figured that out before I did! Might need a bit more prominent sign-posting for this case?📍

Once he finished registration, it dropped him into the repl, not the box he was trying to get to. Should we just [immediately bridge in that case](https://github.com/boldsoftware/exe/issues/74)?📍

Figured that out, logged out, re-ssh'd, now into the box.

Started prodding.

```
uname -a
sudo su -
ip a
```

"pity, no ipv6 address"

"how up to date is this?"

`apt update && apt upgrade -y`

pleased to see that only runc needed updating: "lots of automated setups don't have the latest image, which just wastes time and bandwidth"

"hmmm. the source for my site is public."

"nice, git is already installed..."

git clone

apt install zola (static site generator) failed

"oh, there's a docker. let's try it."

a few bits of tinkering later, dockerized and listening on :8080.

"hmmm, let's see, if i just visit at port 8080..."

more trouble with email on the wrong device. (`browser` command would have fixed this. as would making the box public. but i didn't intervene.) eventually gave up and logged into gmail on the laptop.

"aha, there we go!"

"i like that you have a proxy for https. that's really awesome. let's face it most people just run the letsencrypt script once and then 90 days later it just stops working."

"being able to go from no-idea-what-your-service-is to i-have-my-blog-hosted in 15 minutes is pretty awesome."

reflected that this is like hetzner. i mentioned the lower incremental cost of VMs and his ears perked up. he asked about pricing and pricing model. i tried describing it as "buying part of an underlying machine and then doing what you want with it, via vms", and that seemed to click easily, and have appeal.
