More Shelley feedback, via email.

---

- nit: but I can't "close" conversations
- Like the "Open in VSCode" thing but would be cool if was in VSCode in browser as an option or open in Cursor
- after exploring more, it seems like there is a "welcome" application which is what is running on my port 8000 which ends up mapping to port 80/443.  What is proxying 8000 => 80?
- Perhaps the "welcome" homepage should tell me this is running and hackable from shelley or something?
- nit: if i have multiple windows open and start a new convo, all the windows are not updated with the new convo
- nit: the "diff" for code changes just says `<patches_applied>all</patches_applied>` which isn't particularly helpful
