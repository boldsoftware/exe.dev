Demo by: Crawshaw and Blake

---

David sends exe.dev/welcome

Piper was down / david restarted it

Ssh exe.dev

Does not have github pub key -> Get’s message about making one via keygen

Makes key, names it “exe_dev”

David provides guidance on best way to create key / params

David guides again about where the key was written to (current dir)

Dan now gets email verification ask

Does it quickly. Moves on

Has trouble with “doc” command. Confused. I don’t know how to use doc

NAME in shelly doc url example is confusing. Dan does not know what that should be

Moves to shelly in web UI

“I cannot directly upload files” - shelly

Talking through how to get code to run on VM. Seems to find having to upload files (CSV, python
code, etc) just to run it… “but then I have to give something access to the box?”

David finally shows that there are “other instructions in the output from ‘new’”

Dan has “ah ha” and ssh into box

Sees src and welcomed

Sees Makefile and impulsive runs “make”

Cats makefile

Downloads go1.25.1 (was not already installed)

David and I keep feeling welcomed is too big of a hello world

---

Some takeaways:

On-boarding
    - SSH Gen command help (exists)
    - Missing micro copy
    - Remove mention of billing?
    - Remove billing code? (ask Phil)
    - First encounter could set expectations better
        - You will get a VM for your time and effort
        - Persistent Disk!!!!! Talk about it here
        - Just say Virtual Machine (they know ssh… make assumptions they know what a Virtual Machine is.
        - Stop being passive
        - Can we not ask for email?
        - Or at least defer?
        - Reduce as much friction as possible
        - Make them type “new” (it’s an investment)
        - They should be given clear ROI for doing so though

Value Proposition Notes

    - Persistent Disk
    - Flat-rate for inf VMs
    - Share (via HTTP (and TCP?)) privately or publicly
