Customer: Nate at continue.dev
Exe: Josh

---

They use runloop. It's basically fine, doesn't think about it very much. VM state management is mildly annoying--suspend, resume, etc.

I get the sense he'd work with us mainly to work with us, which is mutual. (Nate: "I thought you weren't interested in transient workloads?" Me: "But I am interested in customers." Nate: "Great!")

Last month he thought he didn't care about pricing, discovered he was wrong when he got his last bill ($6k). Told him we would match or beat runloop one way or another.

Going to send us the interface in his code we need to satisfy, but highlights of what they use / currently have:

- create
	- uses a base image that has their client and a few other things pre-installed on it
	- takes a github url + github token, clones the code into the image
	- accepts env vars, used to pass secrets (github token, continue.dev token, s3 creds)
- await create
	- because promises and typescript
	- likes the immediate response to create followed by later availability, but doesn't really care
	- current latency is a few hundred ms but again doesn't really care, 3s is fine
- create tunnel (== https proxy)
	- long, random subdomain, port they provide
	- no auth (security through obscurity)
- suspend
	- to save money, auto-suspends on inactivity after 48 hours (although as a consequence of looking at their spend he changed this to 2 hours while we spoke--it had hit $5k/mo in vCPU spend)
	- they have to check whether it is running and resume if not
	- adds to their testing matrix
	- excited to delete this and just "start and forget" boxes
	- really wants boxes to just live forever, but knows that for cumulative storage costs reasons knows this is unrealistic, but would also be happy to boxes to be slower to respond the longer it has been since they've had activity
- delete
	- currently never deletes except in response to explicit customer pushing a delete button
	- pricing for this will get untenable (currently snapshots cost them $500/mo and will grow over time), hasn't thought about it yet but needs to soon
	- most important state is stored in s3, so in theory this could be done more proactively, restoring from s3 as needed (this is imperfect, b/c s3 only stores diffs and conversations, not untracked git files, but maybe that's ok?)
- execute command
	- runs a shell command on the server

Currently at 500-1000 dev boxes created/day. runloop is behind an interface so it is easy to throw us in, slowly turn up traffic, etc.

Ready to do a pilot as soon as we are, but also no urgency on their side. I said 4-6 weeks out.

Next move is ours: come back to him with questions, pricing info, or just something to try out.

---

# implementation notes

## what we can do right now

- new --name=uuid --image=foo (or scp up a binary to avoid pulling an image)
- share public
- via ssh to box run git clone

## missing

- env vars / secrets
- gc policy

## questions

- do we leak domain names anywhere (breaking security through obscurity)
- add token (rather than cookie) based auth for https proxy?
- are we ready for the load?
- do we need to split into multiple fake customers for sharding?
- are we confident in uptime etc?
- can we do free trial in near term while we sort out billing and pricing?
