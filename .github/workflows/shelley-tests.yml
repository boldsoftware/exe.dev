name: Shelley Tests

on:
  push:
    branches:
      - main
    paths:
      - "shelley/**"
      - ".github/workflows/shelley-tests.yml"
  workflow_dispatch:
  workflow_call:
    inputs:
      ref:
        description: "Git ref to checkout"
        required: false
        type: string

jobs:
  test-go:
    name: Go Tests
    runs-on: [self-hosted, exe-ci, edric]
    defaults:
      run:
        working-directory: shelley
    steps:
      - name: Check if self-hosted
        id: check-host
        working-directory: .
        run: |
          if hostname -f | grep -qE '\.(exe\.dev|exe\.xyz)$'; then
            echo "self_hosted=true" >> "$GITHUB_OUTPUT"
          elif test `hostname` = edric; then
            echo "self_hosted=true" >> "$GITHUB_OUTPUT"
          else
            echo "self_hosted=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: shelley/go.mod
          cache: false

      - name: Cache Go modules and build
        if: steps.check-host.outputs.self_hosted != 'true'
        uses: actions/cache@v5
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: go-shelley-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('shelley/go.mod') }}-${{ hashFiles('shelley/go.sum') }}
          restore-keys: |
            go-shelley-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('shelley/go.mod') }}-
            go-shelley-${{ runner.os }}-${{ runner.arch }}-

      - name: Set up Node.js and pnpm
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install UI dependencies
        run: |
          cd ui
          pnpm install --frozen-lockfile

      - name: Run TypeScript type check
        run: |
          cd ui
          pnpm run type-check

      - name: Run ESLint
        run: |
          cd ui
          pnpm run lint

      - name: Run unit tests
        run: |
          cd ui
          pnpm test

      - name: Build UI
        run: |
          cd ui
          pnpm run build

      - name: Build template tarballs
        run: make templates

      - name: Verify go generate output unchanged
        run: |
          set -euo pipefail
          echo "Running 'go generate ./...' to verify generated code is up-to-date..."
          go generate ./...
          if [ -n "$(git status --porcelain)" ]; then
            echo "❌ 'go generate ./...' produced uncommitted changes. Commit generated code." >&2
            echo
            echo "Changed files:" >&2
            git status --porcelain >&2
            echo
            echo "Diff:" >&2
            git diff >&2
            exit 1
          fi

      - name: Run go vet
        run: go vet ./...

      - name: Run Go tests
        run: go tool gotestsum --format testname -- -race ./...

  test-ui:
    name: Playwright E2E Tests
    runs-on: [self-hosted, exe-ci, edric]
    defaults:
      run:
        working-directory: shelley
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: shelley/go.mod
          cache: false

      - name: Set up Node.js and pnpm
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install UI dependencies
        run: |
          cd ui
          pnpm install --frozen-lockfile

      - name: Build UI
        run: |
          cd ui
          pnpm run build

      - name: Build template tarballs
        run: make templates

      - name: Install Playwright browsers
        run: |
          cd ui
          $GITHUB_WORKSPACE/bin/retry.sh npx playwright install --with-deps chromium

      - name: Run Playwright E2E tests
        run: |
          cd ui
          npx playwright test

      - name: Publish report to ~/public_html
        if: always()
        run: |
          if ! test "$(hostname)" = edric; then
            exit 0
          fi
          REPORT_DIR="$HOME/public_html/playwright/${{ github.run_number }}"
          mkdir -p "$REPORT_DIR"
          cp -r ui/playwright-report/* "$REPORT_DIR/" 2>/dev/null || true
          if [ -d ui/test-results ] && [ "$(ls ui/test-results 2>/dev/null)" ]; then
            cp -r ui/test-results "$REPORT_DIR/test-results"
          fi
          ln -sfn "$REPORT_DIR" "$HOME/public_html/playwright/latest"
          # Keep last 5 reports
          cd "$HOME/public_html/playwright"
          ls -1d [0-9]* 2>/dev/null | sort -n | head -n -5 | xargs rm -rf 2>/dev/null || true

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: shelley/ui/playwright-report/
          retention-days: 7
          if-no-files-found: ignore

      - name: Upload test results (screenshots, videos, traces)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: shelley/ui/test-results/
          retention-days: 7
          if-no-files-found: ignore

      - name: Job summary
        if: always()
        run: |
          echo '### Playwright E2E Results' >> "$GITHUB_STEP_SUMMARY"
          echo '' >> "$GITHUB_STEP_SUMMARY"
          if test "$(hostname)" = edric; then
            echo "[**View Report**](http://edric/~$(whoami)/playwright/${{ github.run_number }}/)" >> "$GITHUB_STEP_SUMMARY"
            echo '' >> "$GITHUB_STEP_SUMMARY"
          fi
