dist/
/exed
exe.db
exe.db-wal
exe.db-shm
ghuser/whoami.sqlite3*
.vscode/*
!.vscode/
!.vscode/launch.json
!.vscode/tasks.json
node_modules/
.nodeenv/
deps/sshpiper/sshpiperd
deps/sshpiper/metrics
/cmd/exed/exed
*.cover
# codex seems to need a local go cache because of its sandbox.
# it keeps recreating this; stop fighting it.
.gocache
.gomodcache
.gopath
.DS_Store
.cache
tmpkey*
# so i can 'go test -c ./...', and 'go test -race -c && stress foo.test'
*.test

# exelet embedded fs (arch-specific)
exelet/fs/amd64/*
!exelet/fs/amd64/.gitkeep
exelet/fs/arm64/*
!exelet/fs/arm64/.gitkeep
exelet/fs/.hash-*
exelet/kernel/output
exeletd
/exelet-ctl
exelet-fs-*
ops/kernel-builder/output

# data analysis outputs
data/vm-memory-data.csv
data/vm-memory-report.html
snapshot.db

# assorted cmd/* binaries that LLMs like to drop straight in the repo root, natch
blogd
e3e

# https://github.com/wedow/ticket tickets
.tickets
